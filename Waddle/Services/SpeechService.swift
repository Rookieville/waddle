// AVSpeechSynthesizer wrapper — stub for Milestone 1, implemented in Milestone 4

import Foundation

// MARK: - SpeechService

/// Wraps a single reused AVSpeechSynthesizer instance.
/// Speaks phase transition cues ("Run", "Walk", "3 … 2 … 1") during workouts.
/// Never recreate the synthesiser mid-workout — use the single instance here.
/// Implemented in Milestone 4.
final class SpeechService {
    // Speech logic added in Milestone 4.
}
